# hadolint ignore=DL3006
FROM bh.cr/balenalabs/audio-%%BALENA_ARCH%%

# No additional packages needed
# Note: LADSPA equalizer not functional in Alpine 3.15 PulseAudio build

WORKDIR /usr/src
COPY . .

CMD [ "/bin/bash", "/usr/src/start.sh" ]
